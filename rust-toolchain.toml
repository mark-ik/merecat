# Match mere's workspace toolchain (its dependency graph floors at
# rustc >= 1.95; mere pins 1.96 rather than the bare minimum, and merecat
# consumes those same subtrees through the facade).
[toolchain]
channel = "1.96.0"
